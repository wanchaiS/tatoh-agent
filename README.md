### Start virtual env for python 

```bash
source .venv/bin/activate
```

### Install dependencies
```bash
pip install -r requirements.txt
```

### Run the app
```bash
langgraph dev
```

ระบุห้องเจาะจงได้แล้วต้องใช้นการ filter ของวันว่างวันนั้น ระบ room typeได้ด้วย
จำนวนคนไม่บังคับต้องให้
จำนวนคืนยังต้องให้อยู่
ไม่ต้องคอนเฟิมละ 
ถ้าไห้วันชัดเจน ให้หาห้องว่างที่ตีงกับจำนวนคืนในช่วงนั้นก่อน
ถ้าไม่เจอเลย ให้หา ห้องที่รวมกันแล้วได้เท่ากับจำนรวนคืนในช่วงนั้น
ถ้าไม่เจออีกให้โชว์ห้องที่มีว่างอย่างน้อยหนึ่งคืนในช่วงนั้น
ถ้าไม่มีว่างสักห้องเลยในข่วงนั้น ให้ขยายช่วงแล้ววนกลับไปทำแบบเสต๊บแรก 




